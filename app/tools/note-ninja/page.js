import NoteNinja from './NoteNinja';

export const metadata = {
  title: 'NoteNinja | Beyond Dictionary',
  description: 'The elite workspace for writers and developers. Precision typography, instant refinement, and deep analytics.',
};

export default function Page() {
  return <NoteNinja />;
}
